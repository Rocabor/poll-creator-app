# SQLite database (local development). Swap for a PostgreSQL URL to deploy.
DATABASE_URL="file:./dev.db"

# Comma-separated list of request origins allowed to create sessions via the
# guest flow. Keep empty to allow the app's own origin only.
APP_URL="http://localhost:3000"